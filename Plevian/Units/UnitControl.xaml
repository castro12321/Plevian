<UserControl x:Class="Plevian.Units.UnitControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resource="clr-namespace:Plevian.Resource"
             xmlns:local="clr-namespace:Plevian.Units"
             mc:Ignorable="d" 
             >
    <DockPanel Name="stackPanel">
        <Label Content="{Binding Name}" FontSize="14" Width="80"/>
        <resource:ResourceControl DataContext="{Binding Cost}" />
        <Button Content="Recruit" Click="RecruitClicked" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
        <TextBox Name="Quantity" Text="{Binding Quanity, Mode=TwoWay}" Width="50" PreviewTextInput="onPreviewTextInput" TextChanged="quantityChanged" HorizontalAlignment="Right" DockPanel.Dock="Right" />
    </DockPanel>
</UserControl>
